name: ringtone_maker
description: "Ringtone Maker – créez vos sonneries personnalisées"
publish_to: 'none'
version: 1.0.5+32

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  provider: ^6.1.2

  # Fichiers
  file_picker: ^8.1.7
  path_provider: ^2.1.5
  path: ^1.9.0

  # Audio lecture
  audioplayers: ^6.1.0

  # Forme d'onde
  audio_waveforms: ^1.3.0

  # Découpage / export audio (FFmpeg)
  ffmpeg_kit_flutter_new_min: ^3.3.2

  # Synthèse piano (tons purs)
  just_audio: ^0.9.42

  # Définir comme sonnerie Android
  ringtone_set_plus: ^0.0.2+2

  # Achats in-app / abonnements (Google Play / App Store natifs)
  in_app_purchase: ^3.2.3

  # Permissions runtime
  permission_handler: ^11.3.1

  # Stockage préférences (pro/gratuit)
  shared_preferences: ^2.3.3

  # Gestion UUID
  uuid: ^4.5.1
  share_plus: ^12.0.2
  package_info_plus: ^9.0.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: false
  ios: true
  image_path: assets/icon/app_icon.png
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
